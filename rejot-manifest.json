{
  "slug": "my-sync-slug",
  "manifestVersion": 0,
  "connections": [
    {
      "slug": "accounts",
      "config": {
        "connectionType": "postgres",
        "host": "localhost",
        "port": 5435,
        "user": "postgres",
        "password": "postgres",
        "database": "postgres"
      }
    },
    {
      "slug": "orders",
      "config": {
        "connectionType": "postgres",
        "host": "localhost",
        "port": 5436,
        "user": "postgres",
        "password": "postgres",
        "database": "postgres"
      }
    },
    {
      "slug": "eventstore",
      "config": {
        "connectionType": "postgres",
        "host": "localhost",
        "port": 5437,
        "user": "postgres",
        "password": "postgres",
        "database": "postgres"
      }
    }
  ],
  "dataStores": [
    {
      "connectionSlug": "accounts",
      "config": {
        "connectionType": "postgres",
        "publicationName": "rejot_publication",
        "slotName": "rejot_slot"
      }
    },
    {
      "connectionSlug": "orders",
      "config": {
        "connectionType": "postgres",
        "publicationName": "rejot_publication",
        "slotName": "rejot_slot"
      }
    }
  ],
  "eventStores": [
    {
      "connectionSlug": "eventstore"
    }
  ],
  "publicSchemas": [
    {
      "name": "accounts-schema",
      "source": {
        "dataStoreSlug": "accounts",
        "tables": ["accounts"]
      },
      "outputSchema": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "email": {
            "type": "string"
          },
          "created_at": {
            "type": "string",
            "format": "date-time"
          }
        },
        "required": ["id", "name", "email", "created_at"],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "transformations": [
        {
          "transformationType": "postgresql",
          "table": "accounts",
          "sql": "SELECT id, name, email, created_at FROM accounts WHERE id = $1"
        }
      ],
      "version": {
        "major": 1,
        "minor": 0
      },
      "definitionFile": "packages/sync-models/src/schemas.ts"
    }
  ],
  "consumerSchemas": [
    {
      "name": "orders-schema",
      "sourceManifestSlug": "my-sync-slug",
      "publicSchema": {
        "name": "accounts-schema",
        "majorVersion": 1
      },
      "destinationDataStoreSlug": "orders",
      "transformations": [
        {
          "transformationType": "postgresql",
          "sql": "INSERT INTO destination_accounts (id, name, created_at) VALUES (:id, :name, :created_at) ON CONFLICT (id) DO UPDATE SET name = :name, created_at = :created_at"
        }
      ],
      "definitionFile": "packages/sync-models/src/schemas.ts"
    }
  ]
}
